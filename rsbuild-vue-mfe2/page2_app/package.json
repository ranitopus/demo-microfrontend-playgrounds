{
  "name": "page2_app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "serve:dev": "rsbuild dev",
    "build": "vue-tsc -b && rsbuild build",
    "build:watch": "npm run build -- --watch",
    "preview": "rsbuild preview",
    "serve:dev:mfe": "npm run build:watch & sleep 2 && npm run preview"
  },
  "dependencies": {
    "vue": "^3.4.29"
  },
  "devDependencies": {
    "@rsbuild/core": "^1.0.1-beta.5",
    "@rsbuild/plugin-vue": "^1.0.1-beta.5",
    "typescript": "^5.2.2",
    "vue-tsc": "^2.0.21"
  }
}
